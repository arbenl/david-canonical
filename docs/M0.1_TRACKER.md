# DAVID/M0.1 — Master Execution Tracker

**Date:** 2026-06-10
**Status:** ACTIVE
**Maintainer:** Chief Architect (Arben Lila)
**Scope:** Every engineering task between the current state and M0.1 Completion.
**Ground truth:** `thesis_mathematical_core.tex` (June 2026 audit), `unified_tensor_hsmm_framework.tex`, `council_m01/program/david_m01_mathematical_ai_engine_thesis.md`, `ARCHITECTURE.md` (drift-corrected 2026-06-10).

**Conventions.** Status: `DONE` (verified in tree), `PARTIAL`, `TODO`, `BLOCKED(<by>)`. Priority: `P0` = blocks headline routing or violates audited math; `P1` = blocks M0.1 completion; `P2` = quality/polish. Every task lists its acceptance criterion — a task without a checkable criterion is not a task.

**Definition of M0.1 Completion.** All P0+P1 tasks `DONE`; M01-011/012 unblocked with real data; one real 6-month forecast emitted for one (c, p, k) stratum passing FG1–FG6; route ledger signed; everything else routed honestly.

---

## Workstream A — Code Conformance to Audited Math (P0)

The June 2026 audit corrected the theorem documents. The kernels must match the corrected math exactly. Verified state as of 2026-06-10:

| ID | Task | Files | Acceptance criterion | Pri | Status |
|----|------|-------|---------------------|-----|--------|
| A-1 | **J_(3) kernel conformance** — verify `identification_distance_draws` uses third-largest Youden J, no anti-diagonal term | `david/theorems/A_prime.py` | Code review against thesis Theorem A' operational diagnostic | P0 | **DONE** (verified: J_sorted[:, 2], no `\|rho-(1-delta)\|` term) |
| A-2 | **Purge withdrawn-term residue from tests** — stale comment at `test_identification_distance.py:57` still derives expectations from `\|rho-(1-delta)\| ~ 0.39`; re-derive from J_(3) | `tests/test_identification_distance.py` | Comment re-derived from J_(3) ≈ E\|Beta(5,4) − Beta(2,30)\| ≈ 0.49; all tests pass | P0 | **DONE** |
| A-3 | **Anti-regression test: symmetric channel is informative** — assert (rho, delta) = (0.9, 0.1) yields d(θ) = min(0.8, φ-margin), NOT capped near 0.1 by any label-flip term | `tests/test_identification_distance.py` | New test fails against the withdrawn formula, passes against J_(3) | P0 | **DONE** |
| A-4 | **h\* first-crossing conformance** — `horizon_validity()` implements the WITHDRAWN max-form (`if drift_share < tau: h_star = h` keeps extending h\* after a crossing). Replace with `h* = min{h ≥ 1 : drift(h) ≥ τ} − 1`, h\* = h_max if no crossing | `david/theorems/D_forecast_horizon.py` | Unit test with non-monotone drift curve (dip back below τ after first crossing): max-form gives larger h\*, first-crossing form stops at first crossing; F13 re-run | P0 | **DONE** (2026-06-10: `first_crossing_h_star()` landed; non-monotone test shows 3 vs max-form 6; recomputed h\*=5 unchanged on recorded posterior; F13 re-run 0.0739; SBC 100w 0/36 + forecast SBC 0.758/0.941 pass) |
| A-5 | **h\* posterior spread** — report h\* induced by 5%/95% drift envelopes alongside the median, per audit remark "estimated diagnostic, not certified boundary" | `david/theorems/D_forecast_horizon.py`, forecast cell schema | `horizon_validity` payload carries `h_star_q05`, `h_star_q95`; surfaced in §7.1 forecast object | P1 | **DONE** |
| A-6 | **Dependence-adjusted N_eff for FG3 (closes audit open item #6)** — `effective_sample_size(N, I) = N·I²` uses RAW N. Implement Godambe correction `N_eff = N·γ₀/(γ₀ + 2Σγ_h)` with `γ_h/γ₀ = c·Corr(A₀,A_h)`, `c = I²φ(1−φ)/(p(1−p))`, Corr(A₀,A_h) computed from fitted dwell/transition posteriors (renewal functional — NOT a single-λ AR(1) fit), capped at `min(N, N_eff)` | `david/theorems/B_prime.py`, `david/config.py` | (a) New `dependence_adjusted_n_eff()` with unit tests: iid ⇒ N_eff=N; positive serial corr ⇒ N_eff<N; negative ⇒ capped at N. (b) `check_cell` consumes adjusted count. (c) Rename misleading `n_eff_phi_estimate` | P0 | **DONE** |
| A-7 | **FG3 dual-condition audit trail** — gate reason strings must distinguish which condition failed (I-floor vs N_eff·I²-floor) and record both statistics in the route ledger | `david/theorems/B_prime.py`, `david/engine/router.py` | route_ledger cell entries carry `I_lower_95`, `n_eff_i2`, failing condition named | P1 | **DONE** |
| A-8 | **Fix stale module path** — ARCHITECTURE.md references `david/engine/identification_distance.py`; kernel lives at `david/theorems/A_prime.py` | `canonical/docs/ARCHITECTURE.md` | All Implementation: paths in ARCHITECTURE.md resolve to real files | P1 | **DONE** |

---

## Workstream B — SBC Orchestration (Python ↔ Stan)

Per the Phase-2 design: SBC is the integration test of (prior, likelihood, sampler); its power comes from twin parity and dies with it.

| ID | Task | Files | Acceptance criterion | Pri | Status |
|----|------|-------|---------------------|-----|--------|
| B-1 | Measurement SBC loop (θ→world→fit→rank→KS) | `david/simulator/sbc.py`, `david/model/fit.py` | `david sbc --n-worlds 100`: 0/36 params below Bonferroni α/k | P0 | **DONE** (M2: 0/36 fail) |
| B-2 | Bonferroni-corrected SBC gate | `david/config.py` (`SBC_BONFERRONI`), `sbc.py` | effective_alpha = α/n_params computed automatically | P0 | **DONE** (M2) |
| B-3 | Forecast SBC (F14) — history-only fit, coverage of true probability | `david/simulator/forecast_sbc.py` | cov_80 ∈ [0.75, 0.85], cov_95 ∈ [0.90, 0.98] | P0 | **DONE** (0.782 / 0.946) |
| B-4 | **Twin-parity test battery** — moment-match Python prior samples vs Stan generated-quantities prior samples for EVERY parameter (Π softmax rows, ordered alpha_act_col1 order statistics, shifted-Poisson 1+Pois(λ), delta_raw/j_raw links); runs in CI on any change to `synthetic_world.py` or `m01_forward.stan` | `tests/test_twin_parity.py` (new), `stan/synthetic_generator.stan` | Per-parameter KS/moment test Python-vs-Stan prior; CI fails on drift | P0 | **DONE** (2026-06-12: 55-pair battery — KS + Welch mean + Brown-Forsythe variance per pair, Bonferroni α/168 per SBC convention; exhaustiveness guard over every theta entry; emission-consistency check pins link formula to the real `sample_world` code path; drift sensitivity verified by mutation — dwell log(6)→log(5) and 0.9·j_raw both fail loudly) |
| B-5 | **Shared Stan functions via `#include`** — single source of truth for `hsmm_alpha`, `hsmm_right_censored_lpdf` between `m01_forward.stan` and `synthetic_generator.stan`; closes the SBC blind spot (identical-error-in-both) structurally | `stan/functions/hsmm.stanfunctions` (new), both models | Both models compile from the shared include; byte-identical function bodies impossible to drift | P0 | **DONE** (2026-06-12: `stan/functions/hsmm.stanfunctions` contains all 6 HSMM math functions — `shifted_poisson_lpmf/lccdf/rng`, `segment_sum`, `hsmm_alpha`, `hsmm_right_censored_lpdf`; both models include via `#include functions/hsmm.stanfunctions`; `terminal_regime_posterior` stays in `m01_forward.stan` as forward-model-specific; 140/140 tests pass including `test_m01_forward_compiles` and full `test_twin_parity` battery) |
| B-6 | **Rank-histogram shape diagnostics** — per-parameter signature emission: center-clustered ⇒ likelihood overcounting; U-shaped ⇒ prior/width mismatch; skew ⇒ bias. (Would have localized the K-fold selection bug instantly.) | `david/simulator/sbc.py` | SBC summary JSON carries per-param histogram-shape class + χ² bin statistic | P1 | TODO |
| B-7 | SBC artifacts in falsification ledger — typed JSON with seeds, n_worlds, failed params, worst p-values; recorded pass OR fail per Automation Contract | `sbc.py`, `validation/falsification.py` | `falsification_ledger.json` has SBC block for every weekly run | P1 | PARTIAL (summary JSON exists; ledger wiring to verify) |
| B-8 | Weekly cron wiring: `weekly_fit.sh` runs fit + forecast SBC + falsify; SBC fail blocks forecast emission (fail-closed) | `scripts/weekly_fit.sh`, `engine/orchestrator.py` | Induced SBC failure ⇒ `david forecast` refuses to run with typed reason | P0 | PARTIAL |
| B-9 | SBC for the corrected h\*/N_eff kernels — re-run full battery after A-4/A-6 land (kernel changes invalidate prior SBC passes) | — | Post-conformance SBC + F13 re-run recorded in ledger | P0 | BLOCKED(A-6) (post-A-4 SBC + F13 re-run done 2026-06-10; re-run again after A-6) |

---

## Workstream C — Posterior Expected FDP: Envelope + Exceedance (Theorem C / FG6)

| ID | Task | Files | Acceptance criterion | Pri | Status |
|----|------|-------|---------------------|-----|--------|
| C-1 | Marginal expectation rule (sort, prefix scan, m\*, t\*(q)) | `david/theorems/C_renamed.py` | Exactness test: E[FDP] at m\* ≤ q; m\*+1 violates | P0 | **DONE** (verified: cumsum prefix scan, q = 0.10) |
| C-2 | **Claim-eligible family definition** — FDP selection runs ONLY over cells surviving FG1–FG5; family membership pre-registered per destination; excluded cells never enter M | `david/engine/router.py` | Router test: cell routed evidence_gap is absent from p_hat vector passed to C kernel | P0 | **DONE** (2026-06-12: filtering verified at `router.py:169`; `m_claim_eligible` field added to route_ledger; `tests/test_router.py` — 4 tests incl. mock-interception proof that evidence_gap/prior_dominated/horizon_prior_dominated/monitor_only p_active values never enter Theorem C p_hat; 144/144 pass) |
| C-3 | **Sensitivity-envelope FDP** — compute `p_i^- = min_θ p_i^θ` over the pre-registered grid Θ^meas; sort/scan on p^-; guarantees `FDP_env(R) ≤ q_d` at the worst grid point with monotonicity restored | `david/theorems/C_renamed.py`, `david/engine/observability_sensitivity.py` | Test: 3-point grid where naive (single-θ) rule flags a set violating the envelope; conservative rule does not | P0 | TODO |
| C-4 | **FG6 exceedance gate from joint draws** — `FDP^(s) = Σ d_i(1−A_i^(s))/max(1,D)`; accept largest prefix with empirical exceedance fraction ≤ α at γ; downward scan from m\* (exceedance is not monotone in m); Markov fallback q ≤ γα | `david/theorems/C_renamed.py` (new fn), `router.py` | Test with correlated joint draws: expectation rule passes, exceedance rule shrinks the set; both recorded | P0 | **DONE** (2026-06-12: `compute_fdp_exceedance_gate` + `ExceedanceGateResult` in `C_renamed.py`; `FDP_EXCEEDANCE_GAMMA/ALPHA` pre-registered in `config.py`; router loads `headline_draws.npy`, reorders by descending p_i, records both `posterior_fdp` and `exceedance_gate` blocks in ledger with per-cell flags; 20/20 target tests + 157/157 suite pass including non-monotone downward-scan proof) |
| C-5 | **σ(D)-measurability guard** — expectation gate may consume only marginals p_i, never joint draws (the theorem's proof IS this measurability); enforce by API: `compute_posterior_fdp_threshold(p_hat)` keeps its marginal-only signature, exceedance fn takes draws separately | `C_renamed.py` | Code review + docstring contract; no joint-draw arg on the expectation path | P1 | PARTIAL (current API already marginal-only) |
| C-6 | **MCSE/ESS floor on p_i** — require bulk-ESS such that MCSE(p_i) ≪ (q − e_m\*) gate margin; else cell excluded from family with typed reason | `C_renamed.py`, `david/config.py` | Test: low-ESS draws ⇒ cell excluded, reason `fdp_mcse_exceeds_gate_margin` | P1 | TODO |
| C-7 | Ledger fields: t\*(q), m\*, envelope worst-point θ, exceedance fraction, per-cell flagged/unflagged binding reason | `router.py`, route ledger schema | `route_ledger.json` carries full FDP block; no silent cells | P1 | PARTIAL |

---

## Workstream D — Data, Gates, and M01 Blockers (the critical path)

These are the fail-closed states from the thesis packet §12. Nothing in Workstreams A–C unblocks them by itself; they need real data and sign-offs.

| ID | Task | Blocker code | Acceptance criterion | Pri | Status |
|----|------|--------------|---------------------|-----|--------|
| D-1 | Real-fit contract: acquire + adjudicate real evidence for ≥ 1 stratum satisfying S_eff ≥ 3 with replication floor | M01-011 | `real_fit_contract` gates pass; F17 (R̂ ≤ 1.01, ESS, 0 divergences) on real fit | P0 | BLOCKED(data acquisition) |
| D-2 | Forward holdout build: define prospective holdout period (e.g. fit < 2025-01-01, validate ≥ 2025); activates F3/F4/F5/F6/F11/F15 currently SKIP per KNOWN_GATES.md | M01-012 | Holdout config landed; skipped F-tests execute and record pass/fail | P0 | BLOCKED(D-1) |
| D-3 | Gold-label coder calibration on real items (hard gate: uncalibrated tactic ⇒ no headline claims for that tactic) | — | `coder_calibration.stan` κ± posteriors per coder vs `gold_b_calibration.csv`; one-coder-per-family sensitivity re-run | P0 | PARTIAL (model exists; real gold set pending) |
| D-4 | Source-independence ledger initial population + quarterly review process | — | `source_independence_ledger.json` populated; block-collapse K ≥ 3 check wired to FG2 path | P1 | TODO |
| D-5 | External statistician audit package | M01-017 | Requires M01-011 = ready AND M01-012 = pass | P1 | BLOCKED(D-1, D-2) |
| D-6 | Legal language sign-off (claim-firewall texts; routes never emit legal/policy/causal) | M01-018 | Counsel review of all reviewer-facing strings | P1 | BLOCKED |
| D-7 | Pre-merge review + replication package (`david replay --run-id`) | M01-019/020 | Independent dry-run reproduces a full run from raw data + recorded versions | P1 | BLOCKED(D-1) |
| D-8 | F13 expected-fail documentation stays synchronized after A-4 lands (first-crossing h\* may change the 63-cell horizon_prior_dominated count) | — | KNOWN_GATES.md F13 section regenerated from the post-A-4 ledger | P1 | **DONE** (2026-06-10: post-A-4 falsify re-run; h\*=5 and statistic 0.0739 unchanged — first-crossing and max-form coincide on current posterior; KNOWN_GATES.md F13 section updated with verification note) |

---

## Workstream E — `david-ui/`: The Mathematical Audit Dashboard (Premium Mandate)

**Doctrine:** the UI is a *renderer of the route ledger*, never a second implementation of the math. It must make gate states impossible to miss and impossible to misread. Verified current state: Next 15 / React 19 / Tailwind 4 / recharts / swr; 12 client components, 0 uses of `React.cache()` or server-only data layer — the app is client-heavy and must be inverted to Server Components.

| ID | Task | Files | Acceptance criterion | Pri | Status |
|----|------|-------|---------------------|-----|--------|
| E-1 | **Route-typed rendering contract** — discriminated union `RouteState = headline \| monitor_only \| aggregate_only \| evidence_gap \| withhold \| horizon_prior_dominated \| prior_dominated`; every cell visualization takes `RouteState`, exhaustive switch, NO component can render a chart without a route. TypeScript enforces the claim firewall at compile time | `src/lib/route-state.ts` (new), all chart components | `tsc` fails if a route variant is unhandled; no chart component accepts raw numbers without a route wrapper | P0 | TODO |
| E-2 | **FG5 mathematical grey-out** — trajectory line chart renders conditional forecast in full color for h ≤ h\*; for h > h\*, the line VISUALLY REVERTS to the stationary prior π∞ (flat reference band, desaturated grey), with the h\* boundary drawn as a labeled vertical rule and the h\* posterior spread (A-5) as a shaded uncertainty band. The greyed segment plots π∞ ± its posterior interval — i.e. what the engine actually emits — never the extrapolated conditional line | `src/components/premium-forecast-chart.tsx` | Snapshot test: cell with h\* = 5, horizons {3,6,9,12} ⇒ h=3 colored conditional; h ∈ {6,9,12} grey flat at π∞; rule + band at h\* | P0 | TODO |
| E-3 | **EVIDENCE_GAP warning block** — when Opportunity-Frame Abstention triggers (Q_g unknown/unbounded) or route = evidence_gap, render a full-width, high-contrast `EVIDENCE_GAP` block: monospace route code, the binding reasons Λ_g verbatim from the ledger, the failed gate chain, and the theorem citation (e.g. "Theorem 1: Opportunity-Frame Abstention"). NEVER an empty chart, NEVER a zero-filled chart | `src/components/evidence-gap-block.tsx` (new) | Snapshot test: evidence_gap cell ⇒ block with route code + ≥ 1 binding reason; chart component is not mounted | P0 | TODO |
| E-4 | **Gate-stack panel (FG1–FG6)** — per-cell vertical gate ladder showing each gate's statistic vs floor (d(θ) median vs 0.05; I_lower_95 vs 0.10; N_eff·I² vs 3.0; h vs h\*; FDP vs q; exceedance vs α), pass/fail/skip states from falsification + route ledgers; F13 expected-fail renders with the KNOWN_GATES explainer, not as an error | extend `src/components/gate-card.tsx`, `strata-health.tsx` | Every headline cell shows 6 green gates with the actual numbers; F13 fail shows "expected under current regime" callout | P0 | TODO |
| E-5 | **FDP threshold visualization** — sorted p_(j) bar chart with the t\*(q) cutline, the running e_m curve against q = 0.10, flagged prefix highlighted; envelope worst-point and FG6 exceedance fraction displayed | `src/components/fdp-threshold-chart.tsx` (new) | Renders from route_ledger FDP block (C-7) only; no client-side recomputation of m\* | P1 | BLOCKED(C-7) |
| E-6 | **Server Components migration** — pages become async Server Components reading run artifacts (route_ledger.json, forecast cells) server-side; per-request dedup via `React.cache()` around the ledger reader; `server-only` package on the data layer; client components retained ONLY for interactivity (adjudicator queue, approve button, tooltips); remove swr polling for immutable per-run artifacts | `src/lib/api.ts` → `src/lib/data.ts` (server), `src/app/*/page.tsx` | 0 `use client` on route pages; ledger read once per request (React.cache); Lighthouse TTI improves; immutable runs served from RSC payload | P1 | TODO (verified: currently 12 client components, 0 React.cache) |
| E-7 | **Premium technical aesthetic** — dark engineering-console theme; tabular-nums monospace for all statistics; per-cell badges for d(θ), I(O), N_eff·I², h\*; route codes rendered as typed chips; recharts themed to the console palette; zero decorative chartjunk — every pixel encodes a gate state or a posterior quantity | `src/app/globals.css`, all components | Design review: every displayed number traceable to a ledger field; consistent type scale | P2 | PARTIAL (premium components exist; theming pass pending) |
| E-8 | **Read-only enforcement** — UI cannot mutate routes, thresholds, or gate outcomes; the approve button signs the route ledger (M01 human review) and nothing else; API layer read-only per ARCHITECTURE §7 | `src/app/api/*`, `approve-button.tsx` | API surface audit: no endpoint writes to fits/forecasts; approve writes only the sign-off record | P0 | PARTIAL |
| E-9 | **Run-provenance footer** — every page shows model_version, code_version, data_cutoff, pre_registration_version, theorem packet SHAs from the run artifact | `src/components/provenance-footer.tsx` (new) | Footer renders all 5 fields for the displayed run; mismatched versions render a warning chip | P2 | TODO |

---

## Workstream F — Process & Documentation

| ID | Task | Acceptance criterion | Pri | Status |
|----|------|---------------------|-----|--------|
| F-1 | ARCHITECTURE.md drift corrections (d(θ)/J_(3), I lower-95, h\* first-crossing, open item #6) | Doc matches audited thesis core | P0 | **DONE** (2026-06-10) |
| F-2 | LaTeX publication rule compliance: formal math docs in `.tex` + TikZ/pgfplots; markdown only as index/summary (this tracker is an index — compliant) | No new formal math content lands as markdown | P1 | ONGOING |
| F-3 | Re-verify `%%NEEDS_HUMAN_MATH_REVIEW%%` markers: author sign-off pass over thesis_mathematical_core.tex audit edits | All markers resolved or escalated | P1 | TODO |
| F-4 | Tracker hygiene: this file updated whenever a task changes state; stale > 14 days triggers review | `git log` shows updates tracking the work | P1 | ONGOING |
| F-5 | **Pre-existing test suite defects** — tests failing unrelated to recent changes: `test_ingest_layer` (normalization mismatches), `test_m01_forward_compiles` (missing `m01_forward.stan`), and `test_smoke_sbc` (ValueErrors). Logged per SOP Phase 4 | `tests/` | All pre-existing failures resolved and green | P1 | **DONE** |

---

## Critical path to M0.1 Completion

```
A-4, A-6 (kernel conformance)
   └─> B-9 (re-run SBC + F13 on corrected kernels)
C-2, C-3, C-4 (FDP envelope + exceedance)        E-1..E-4 (UI gate enforcement)
   └─> C-7 (ledger fields) ─> E-5 (FDP viz)
D-1 (real data) ─> D-2 (holdout) ─> D-5/D-6/D-7 (audits, sign-offs, replication)
   └─> first real 6-month forecast passing FG1–FG6 ─> route ledger sign-off ─> M0.1 COMPLETE
```

The single longest pole is **D-1 (real data acquisition)** — everything in Workstreams A–C and E can and should land before it, so that when M01-011 closes, the corrected, fully-gated, fully-rendered pipeline is waiting for it.
