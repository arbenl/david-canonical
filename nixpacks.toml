# Nixpacks build + runtime configuration for Railway.
# aptPkgs are included in both the build image AND the final runtime container.
# make + g++ are needed by CmdStan to compile Stan models → C++ binaries.
[phases.setup]
aptPkgs = ["make", "g++", "libstdc++-12-dev"]
