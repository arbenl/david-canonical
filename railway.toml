[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn david.api.server:api --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Runs once after each deploy — creates/migrates DB schema idempotently
releaseCommand = "python -m david.cli db init"
